type RankType = {
  id: number;
  name: string;
  rank: number;
  description: string;
};

export type RankPostType = {
  name: string;
  rank: number;
  description: string;
}

export default RankType;